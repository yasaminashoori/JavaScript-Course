const totalPrice = 220_000;
const discountLimit = 200_000;
// console.log(totalPrice > discountLimit);  //True: reulst of comparison is bool
const discount = totalPrice > discountLimit ? "10%" :"5%";
console.log(discount)
