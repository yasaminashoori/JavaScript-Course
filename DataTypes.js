// Datatypes

const age = 12;  // NUmber: int, float

const userName = "Yas"; //string: chars, text

const isActive = true; //Boolean: true, false

let favorites //undefined

const likedPosts = null; //Null: empty value or nothing

// Symbol: Unique value and unchangable
// BigInt: numeric value and too large

let userAge = 28;

let userName2 = "Yasamin";

const isAlive = true;

let users;

const archivedPosts = null;

console.log(userAge, userName);

console.log(typeof userAge);

console.log(users);

console.log(typeof archivedPosts);